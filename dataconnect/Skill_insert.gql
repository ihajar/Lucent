# This is a file for you to write an un-named mutation. 
# Only one un-named mutation is allowed per file.
mutation {
  skill_insert(
    data: {
      authorClerkId: "user_3CwrtgNEUBOufBuiZegW7HT9yfz", 
      createdAt: "2025-10-25T09:45:00Z", 
      description: "Learn fast unit testing with Vitest, including mocking, snapshot testing, and React component testing.",
      installCommand: "npm install -D vitest @vitest/ui",  
      promptConfig: "You are a testing expert who writes comprehensive, maintainable tests using Vitest and modern testing patterns. Write Vitest tests for {componentName} covering edge cases, mocking, and async behavior. Current tests use Jest. Migrate this test suite to Vitest syntax and improve coverage. Output Format: Complete test file with describe/it blocks, mocks, and assertions.", 
      tags: ["vitest", "testing", "unit-testing", "jest-alternative"], 
      title: "Vitest Unit Testing",
      usageExample: "import { describe, it, expect, vi } from 'vitest';\nimport { render, screen } from '@testing-library/react';\nimport userEvent from '@testing-library/user-event';\nimport Counter from './Counter';\n\ndescribe('Counter', () => {\n  it('increments when button is clicked', async () => {\n    render(<Counter />);\n    const button = screen.getByRole('button', { name: /increment/i });\n    \n    await userEvent.click(button);\n    \n    expect(screen.getByText('Count: 1')).toBeDefined();\n  });\n\n  it('handles async data fetching', async () => {\n    const mockFetch = vi.fn().mockResolvedValue({ data: 'test' });\n    render(<DataComponent fetchData={mockFetch} />);\n    \n    expect(mockFetch).toHaveBeenCalledTimes(1);\n  });\n});"
  }
  )
}